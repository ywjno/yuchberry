#sidebar Sidebar
= 改进列表 =
<wiki:toc max_depth="2"/>

    就是一个功能列表，我把每次朋友们提出的改进方案和Bug，还有我想要改的东西都写到这里面，然后改好的，就用划掉一个～

== beta 之后 ==

  具体的beta测试报告在[http://code.google.com/p/yuchberry/wiki/Beta_Report 这里]
    
   # ~~ 加上启动快捷键Y ~~
   # ~~ 服务器需要加上“到期时间提醒邮件”发送功能 ~~
   # ~~ 客户端加入开机启动的选项 ~~
   # ~~ 集成配置，加入一个帐户的时候，自动选择这个用户，并滚动到这个用户 ~~
   # ~~ yuchberry 添加附件改成 YB 添加附件~~
   # ~~ 服务器查找用户的功能~~
   # ~~ 显示是否有客户端连接上来的状态的功能~~
   # 看看能不能内嵌HTML的图片
   # ~~ 把配置信息复制一份到ROM里面～不然插拔usb的时候老是读不出配置 ~~
   # ~~ 如果发送陌生信箱没有问题，如果是联系人（中文）的名单，就有问题，发送不出 ~~
   # ~~ 8900，5.0系统，铃声有问题。要把提醒做到系统里面去 ~~
   # ~~ 附件添加成功加上提示 ~~
   # ~~ 有时间实现一下附加下载进度条~~
   # ~~ 加上一个流量统计，上行流量和下行流量 ~~
   # ~~ 更改转发邮件无法转发附件的问题 ~~
   # ~~ 让用户自定义心跳包的间隔，同时加上收信的时间表 ~~
   # ~~ 添加地理位置信息 ~~
   # 在 RAiN 的基础代码上试图解决一下断网问题
   # 试试将邮箱密码转换成密文保存在本地，用用户密码来进行加解密算子。
   # ~~ 加上后台执行服务器的bat ~~
   # ~~ 加入调用黑莓自带的程序查看图片的代码 ~~

== 0.9.442 之后==

   # ~~ 论坛邮件无法带原文的问题~~
   # ~~ 推送两次邮件，间隔很短的问题（由于正好处于重复推送的时间点，不是以每封邮件为准的）~~
   # 37middle@sina.com的邮箱 too many logins的问题，重复的推送，不重新登录，需要看看～
   # ~~ 增加上下统计流量的总和 的统计 ~~
   # 试着将PushInterval 加到每个账户里面去～
   # ~~ 将服务器生成HTML的时候，把charset都该成UTF8的 ~~
   # ~~ 如果删除未读邮件，小红灯不会停止闪烁，同时会导致内存泄露问题 ~~
   # ~~ 如果发送的时候断开连接，可以标记上Pending标志，不要让 X 留在那里。~~
   # 邮件的SimpleHash在服务器算的时候如果不是UTF8的编码，那会出现问题，但是模拟器却不会出现，是为什么呢？
   # ~~ 现在有2个地方耗电：一是每5分钟的写入ROM流量统计，二是地理位置信息的刷新。都要解决！~~
   # 标题过长的信件在手机上的哈希值不对，但是在模拟器上的值却是好的。在把那几封邮件测试一下
   # ~~ 貌似附件的路径指向的是内存设备，检查一下 ~~
   # ~~ 把随开机启动SLEEP的时间再设置长一点，好像手机启动时间更长 ~~
   # 检查一下9630 5.0OS的链接配置
   # ~~ 写一个收发邮件的原理流程图 ~~
   # ~~ 解决一些手机上出现的Bad Parameter 无法连接的问题~~
   # ~~ 解决客户端重复登陆导致的系统资源泄漏问题 ~~

== 1.0.584 之后== 

   # ~~ 修改流量统计时候的递归调用，重复增加TCP连接时候的流量消耗 ~~
   # ~~ 增加“发送邮件”、“接收邮件”的统计功能。~~
   # ~~ 增加“诊断信息”可以复制到剪切板的功能。~~
   # ~~ 将每个邮件推送账户都有发件名称的功能，而不是赤裸裸的邮件地址。~~
   # ~~ 增加移动网络自动切换到wifi的功能。~~ (需要找人测试一下)
   # ~~ HTML 转化去掉多余的换行，去掉注释。 ~~
   # ~~ 当“答复：Re”混合时，就会出现问题，回复邮件的时候，系统“答复”前缀就不会自动添加了。~~
   # ~~ 增加一个Email字段，可以填写发件人的名字默认是空 ~~
   # ~~ 增加一个字段标记是否在服务器启动的时候查收之前的未读邮件。~~
   # ~~ 长按挂机键关机之后\GPRS服务不可用（也没有wifi），事实上YB还在运行，但是没有信号了，需要探测是否有信号，才能开启连接 ~~
   # 有些机器型号在接受数据的时候，没有办法发送数据，需要避免一下，虽然全双工的TCP不是这样的。
   # ~~ 加上“答复”是否带原文的选项。因为这些都是要流量的。~~
   # ~~ 加上一个选项，删除邮件的时候，把邮箱里面的邮件也删除了 ~~
   # ~~ 发送一封邮件，就可以更改服务器的签名 ~~
   # ~~ 增加“集成配置工具”可以手动更改到期时间 ~~
   # ~~ 增加 最多接受多少字节的邮件的功能。 ~~
   # ~~ 修改一下这个[http://code.google.com/p/yuchberry/issues/detail?id=124&colspec=ID%20Type%20Status%20Priority%20Stars%20Summary Issue]。 ~~
   # ~~ 有时间做一下local化，中英文并存的提示（就是在收取到非标准信件时的异常提示）~~
   # ~~ 将在yuchberry主界面上的发送附件进度放到标题之前，而不是之后。~~
   # ~~ 添加设置1分钟心跳间隔时的耗电提醒。~~
   # ~~ 在客户端的主界面上加上“帮助”菜单，写一篇wiki文档，专门放给这个菜单。~~
   # ~~ 加一个字段，标明是否在发送邮件时自动复制一份放到Sent文件夹下面，貌似除了GMail之外，其他邮箱的IMAP/POP3没有这个功能。~~
   # ~~ 打开附件的时候无法选择内置程序打开，ContainHandler有问题。模拟器上可以，实际机器上没有。 ~~
   # ~~ 发送附件超过1个之后，无法发送成功的bug ~~
   # ~~ 重复收信时，不添加到消息程序、不触发通知 ~~
   

== 1.1.715 之后 （1.2.875更新内容）== 
   # 增加 config.xml里面的 yuchberry标签，具体干什么，你懂得。
   # 更新提示机制有问题啊，如果之前旧的服务器获得了更新标志，新的客户端链接上来，就会报错，所以，必须在每次链接上来的时候，服务器去取更新标志。
   # ~~ 使用“答复时丢弃原文”的时候，不替换“答复：”等前缀了。 ~~
   # ~~ 解决自驾服务器时config.xml明文密码的问题！~~
   # 试图解决：Server chose unsupported or disabled protocol: SSLv3 （zhxu@neusoft.com）其实就是增加STL的支持。区分一下smtp，smtps(sl),smtpt(ls)
   # POP3 不支持复制已发送邮件到文件夹（javax.mail.MethodNotSupportedException: Append not supported）
   # 英文版的 YuchSign 验证码。
   # ~~ 同一个邮件中有相同名字的附件，会出现问题。 ~~
   # 看一下能不能加入在程序里面设置工作目录的选项，方便linux用户。
   # ~~ 中文名+邮件地址 发送邮件时，会出现乱码的问题~~
   # ~~ qq信箱的IMAP无法连接的问题 (可以使用imap 的143端口连接了)~~
   # ~~ 6.0 系统转发邮件没有原文 ~~
   # ~~ 客户端主机地址中的空格需要去掉，或者提示 ~~
   # ~~ 发送带有附件的邮件的时候，会在手机本地将内容重复一遍。~~
   # 解决一下[http://code.google.com/p/yuchberry/issues/detail?id=158 这个Issue]
   # yuchweibo
    # ~~ Space下翻到最后条时页面变灰无法选择条目~~
    # ~~ 头像只能展开才能看见可以提到合并时就可以看见~~
    # ~~ 一旦有推送新条目就白屏~~
    # ~~ Timeline与@me只能看6条 （9700 OS6）~~
    # ~~ debug info 里面使用帮助菜单弹出浏览器会出错。高级设置、用户状态也有可能。~~
    # ~~ 翻到对上面的时候不应该出现发weibo的窗口（Home）~~
    # ~~ 使用一次性计算空格的长度，保证“关注（L）”“--来自于”不重叠~~
    # ~~ 出一个配置，可以显示、隐藏“--来自于”和“转发”“@Weibo”“收藏”几个按钮。~~
    # ~~做一个是否需要下载头像的配置，以适应小内存的机器 ~~
    # ~~加一个滚动条 ~~

== 1.2.875 之后 （1.3.926更新内容）==
   # ~~新发送推可以加入快捷弹出新窗口，这个任何时候发推都不会影响查看倒推地标记 ~~
   # 已读与未读标记
   # ~~在消息模块中增加菜单，可以发Weibo ~~
   # ~~ 发完Weibo之后，成功需要提示一下。 ~~
   # 看看能否显示有图有真相的小图
   # ~~ 浏览查看Weibo列表时，需要去掉上面的标题栏 ~~
   # ~~5.0版本的头像需要显示50像素大小的 ~~
   # ~~ 收到回复、评论时图标要变 ~~
   # ~~ 看看能不能使用代理模式授权访问 ~~
   # ~~ 更改加密密码的界面为命令行界面 ~~
   # ~~ 当有Item正在编辑时不增加，不删除 ~~
   # ~~ qq的认证不是V，改一下 ~~
   # ~~ 解决一下如何手动刷新配置的问题 ~~
   # ~~ 删除自己的评论可能会出问题 ~~
   # ~~ 发完weibo，判断，如果是手动刷新的，就要刷新一次 ~~
   # ~~ 发送邮件的抄送、迷送方收不到邮件 ~~
   # ~~ 增加YB断开提醒的配置和功能。 ~~

== 1.3.926 之后==
   # ~~ 在列表里面查看本Weibo所有内容 ~~ 
   # ~~ 尽力做到解析内容里面的 http 连接 ~~
   # ~~ 在观看模式中 按T 也需要回到顶上 ~~
   # ~~ 6.0 的弹窗发Weibo，无法显示的问题 ~~
   # 试试Weibo私信放到消息文件夹里面看看。
   # ~~ 流量统计下面加一个复制到剪贴板的按钮和功能。 ~~
   # 修改一下Weibo推送顺序混乱的bug。
   # ~~ 转发、@ 的时候最大字数加大，不消除尾部，发送时自动截取，最好能显示字数。 ~~
   # ~~ 把QQ微博的URL提取一下，太坑爹了 ~~
   # ~~ 微博的来自那里，如果是空的，就不显示了，不过得看看为啥会为空 ~~
   # ~~ 评论的微博，如果是自己的，就别添加关注按钮了 ~~
   # ~~ QQ Weibo 无法看图，查查那个URL是否有效 ~~
   # ~~ 看看能否显示有图有真相的小图 ~~
   # ~~ Sina回复评论有些问题，貌似题不对文 ~~ 
   # ~~ 把断开提醒改成“若是链接不上，尝试5次后提醒” ~~ 
   # ~~ 保存数据的时候加一个提醒框，说正在“保存数据，请稍等……” ~~ 
   # ~~ 自己发出的RT，@ 不用提醒 ~~ 
   # 加一个选项，不保存历史Weibo到内存里面
   # 一键转发qq，t微博
   # ~~ 邮件附件列表需要排序一下，最近的文件放在前头 ~~ 
   # ~~ 保存打开图像时候的配置 ~~ 
   # ~~ YuchSign需要加一个重新发送激活邮件的按钮 ~~ 
   # 如果APN有空格，回车的时候要提醒，或者干脆禁用这两个字符
   # 列表模式的时候按E和F要起作用
   
 