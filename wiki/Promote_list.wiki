= 改进列表 =

就是一个功能列表，我把每次朋友们提出的改进方案和Bug，还有我想要改的东西都写到这里面，然后改好的，就用划掉一个～

== beta 之后 ==

   # ~~ 加上启动快捷键Y ~~
   # ~~ 服务器需要加上“到期时间提醒邮件”发送功能 ~~
   # ~~ 客户端加入开机启动的选项 ~~
   # ~~ 集成配置，加入一个帐户的时候，自动选择这个用户，并滚动到这个用户 ~~
   # ~~ yuchberry 添加附件改成 YB 添加附件~~
   # ~~ 服务器查找用户的功能~~
   # ~~ 显示是否有客户端连接上来的状态的功能~~
   # 看看能不能内嵌HTML的图片
   # ~~ 把配置信息复制一份到ROM里面～不然插拔usb的时候老是读不出配置 ~~
   # ~~ 如果发送陌生信箱没有问题，如果是联系人（中文）的名单，就有问题，发送不出 ~~
   # ~~ 8900，5.0系统，铃声有问题。要把提醒做到系统里面去 ~~
   # ~~ 附件添加成功加上提示 ~~
   # ~~ 有时间实现一下附加下载进度条~~
   # ~~ 加上一个流量统计，上行流量和下行流量 ~~
   # ~~ 更改转发邮件无法转发附件的问题 ~~
   # ~~ 让用户自定义心跳包的间隔，同时加上收信的时间表 ~~
   # ~~ 添加地理位置信息 ~~
   # 在 RAiN 的基础代码上试图解决一下断网问题
   # 试试将邮箱密码转换成密文保存在本地，用用户密码来进行加解密算子。
   # ~~ 加上后台执行服务器的bat ~~
   # ~~ 加入调用黑莓自带的程序查看图片的代码 ~~

== 0.9.442 之后==

   # ~~ 论坛邮件无法带原文的问题~~
   # ~~ 推送两次邮件，间隔很短的问题（由于正好处于重复推送的时间点，不是以每封邮件为准的）~~
   # 37middle@sina.com的邮箱 too many logins的问题，重复的推送，不重新登录，需要看看～
   # ~~ 增加上下统计流量的总和 的统计 ~~
   # 试着将PushInterval 加到每个账户里面去～
   # ~~ 将服务器生成HTML的时候，把charset都该成UTF8的 ~~
   # ~~ 如果删除未读邮件，小红灯不会停止闪烁，同时会导致内存泄露问题 ~~
   # ~~ 如果发送的时候断开连接，可以标记上Pending标志，不要让 X 留在那里。~~
   # 邮件的SimpleHash在服务器算的时候如果不是UTF8的编码，那会出现问题，但是模拟器却不会出现，是为什么呢？
   # ~~ 现在有2个地方耗电：一是每5分钟的写入ROM流量统计，二是地理位置信息的刷新。都要解决！~~
   # 标题过长的信件在手机上的哈希值不对，但是在模拟器上的值却是好的。在把那几封邮件测试一下
   # ~~ 貌似附件的路径指向的是内存设备，检查一下 ~~
   # ~~ 把随开机启动SLEEP的时间再设置长一点，好像手机启动时间更长 ~~
   # 检查一下9630 5.0OS的链接配置
   # ~~ 写一个发邮件的原理流程图 ~~
   # ~~ 解决一些手机上出现的Bad Parameter 无法连接的问题~~