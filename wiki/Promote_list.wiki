#sidebar Sidebar
= 改进列表（版本更新内容） =
<wiki:toc max_depth="2"/>
    有任何建议或者问题，请添加到[http://code.google.com/p/yuchberry/issues/list Issues]，或者发送邮件到 yuchberry@gmail.com ，<b>请不要在此回复评论</b>。
== 正在改进…… ==
  # ~~ config.xml 登录名和邮件地址分开 ~~
  # ~~ config.xml 加入自定义XMPP服务器、端口的功能 ~~
  # ~~ 单独发布加密密码工具，下载就能用，不要下载jre的那种。~~
  # 看看[http://code.google.com/p/yuchberry/issues/detail?id=221&colspec=ID%20Type%20Status%20Priority%20Stars%20Summary 这些问题]
  * YuchBerry
   # ~~ 试图加入Weibo相互关注的状态 ~~ ，同时可以给对方发起私信
   # ~~ 看看是否可以加入微薄的关注者备注的功能 ~~
   # ~~ 在编写邮件的界面加入可以选择发送人邮箱的功能 ~~
   # ~~ IM对话中，title加入头像 ~~
   # ~~ Weibo转发、评论不带原文的选项 （仅转发，仅评论）~~
   # ~~ QQWeibo私信重复推送的问题 ~~
   # ~~ 选择表情的时候加入放大的图，不然分辨率太大的机器看不到小图标 ~~
   # ~~ 发布微博成功等信息也放入到调试信息里面 ~~
   # ~~ 优先使用Email(CMIME)条目推送邮件（当然，如果没有语盒的CMIME还是可以使用任意的CMIME）,加入多个条目时候使用某个条目的提示~~
   # ~~ 去掉后台图标(增加配置选项) ~~
   # ~~ IM模块：增加光标不在输入框是快捷键最顶，最底功能 ~~
   # ~~ 增加IM分组的显示功能 ~~
   # ~~ 增加时区的区分，IM和Weibo的时间显示必须按照本地系统的时区 ~~ (这个原来就是按照时区显示的)
   # ~~ 探测到系统有多个SB的时候，弹出提示说语盒使用了哪一个 ~~
   # ~~ 解决IM聊天，发微博时无故发送图片的问题 ~~
   # ~~ IM模块与非语盒用户聊天时，提供文件服务（传输图片和声音） ~~
   # ~~ 添加附件窗口提供图片的预览 ~~
   # ~~ 添加附件窗口查看大附件图片容易内存不够使，看看为什么。~~
   # ~~ IM有新消息的时候，精确到具体联系人 ~~
   # 选择“优先使用wifi”的时候心跳检测是否有wifi可用，如果可用，就断开使用wifi
   # ~~ 新的用户，自动选择 APN 填写上 ~~
   # ~~ T weibo 的一键 RT 功能 ~~
   # ~~ IM 弹窗提醒后，聊天时提醒（响铃）重复的问题 ~~
  * YuchDroid
   # 加入Weibo模块
   # 回复全部的选项需要加入以下，比如你收到有风邮件，也有cc到别人的，也需要回复到
   # 批量删除邮件
   # 向左划邮件删除（右边空档出现“删除”二字），向右划已读（左边空档出现“已读”二字）

== 1.11.1633更新内容 ==
  * YuchBerry
   # ~~ 需要把浏览文件（附件）的模块写成延迟加载的，不然浏览几百号文件的文件夹就会卡死 ~~
   # ~~ IM发图片加一个浏览文件的功能 ~~
   # ~~ 想办法解决weibo 评论一个不带@别人的功能。 ~~
   # ~~ 6.0 系统的IM聊天界面聊天信息无法滑到最近一条（无论是正着的，还是倒立的） ~~
   # ~~ Weibo 加一个 RT TO ALL 的功能，就是新建一个微博，把原始博客的所有人都放到这里面，不要内容。 ~~
   # ~~ 发送到不同微博的功能 ~~
   # ~~ 去除手动断开连接时候的弹窗连接失败的提示 ~~
   # ~~ 兼容性的加上主屏幕微博、IM的提示。 ~~


== 1.10.1556更新内容 ==
   # ~~ 邮件解决裂帖问题 （邮件组讨论不断新建帖子的问题）~~
   # ~~ 邮件发送失败之后提醒用户 ~~
   # ~~ 优化附件下载 ~~
   # ~~ 密码端口错误的对话框很是坑爹，会重复弹出卡死客户端 ~~
   # ~~ 非官方服务器的微博界面、IM尝试加上广告显示 ~~ 最后的结果见[http://blog.yuchs.com/?p=100 这里]——不加了。

 * YuchBerry
  # Weibo查看图片时，不自动加载已经推送但是没有展开的微博（源于Asa的一个提议）
  # ~~ 加入短信分享微博功能 ~~ 
  # ~~ 微博做一个配置转发不带原文，不然会@一次别人~~ (并没更改，在选择<b>跨微博转发</b>的时候必须带有转发内容)
  # ~~ 微博名字过长遮挡住后面的东西 ~~
  # ~~ 增加对del键的一些相关操作支持,例如在聊天记录的界面能够删除相应与好友的聊天框，在状态界面能够删除某条状态 ~~
  # ~~ 微博发送成功之后，Update Weibo OK！可以改成按照设定语言匹配 ~~
  # ~~ 微博精简模式中不显示头像 ~~
  # ~~ 在weibo中添加取消关注的功能 ~~
  # ~~ IM中多消息重复到达时，重复提醒的配置 ~~
  # ~~ 打开邮件附件、打开微博上传文件时需要加一个【上一次打开的路径】的功能 ~~
  # ~~ weibo查看资料的时候把性别显示一下 ~~
  # ~~ 表情选择的时候，向下滚动会滚歪 ~~
  # ~~ 去掉IM中@的解析 ~~ （修改了一半，实在不好更改）
  # ~~ weibo加入一个位置按钮，使得发布时可以选择是否发布位置，当然，weibo配置里面是需要打开位置共享 ~~
  # ~~ [http://code.google.com/p/yuchberry/issues/detail?id=196&colspec=ID%20Type%20Status%20Priority%20Stars%20Summary 这个问题]（在YuchIM聊天界面默認調用是左右“aA+Enter”,期望以“左aA+Enter”作為默認，釋放右aA的佔用）解决一下。 ~~

 * YuchDroid
  # ~~ 草稿编辑之后，再次编辑点击发送，发送的是之前的版本 ~~
  # ~~ 加入附件上传，下载功能 ~~ 
  # 完成新手向导功能
  # ~~ 加入直接在手机上注册使用的功能 ~~ 
  # ~~ 加入自动更新的机制 ~~ （由Android Market代劳吧）
  # ~~ 处理一下复制邮件文本的问题~~ 
  # ~~  打开邮件组，点击邮件头，在点击下一个邮件组按钮换组，再点击邮件头，出现的信息不一致~~ 
  # ~~ 发送按钮和保存草稿按钮互换一下，不然输入的时候会出错~~ 
  # ~~ 邮件头的显示，如果收件人多过一行，会与标题重叠~~ 
  # ~~ 邮件头点开显示没有吧Reply-To的信息显示出来~~
  # ~~ 周期性删除历史邮件，不然数据库会越来越大，导致软件速度很慢 ~~ 
  
== 1.9.1405更新内容 ==
   # ~~ IM 当前状态家点花样出来，不然难以辨认 ~~
   # ~~ IM 聊天信息过长，显示不全的问题。~~ 
   # ~~ IM 好友列表，两个不同的source，就会有两个重复的好友帐号~~ 
   # ~~ IM 保存图片语音的配置没有写入配置~~ 
   # ~~ 如果关闭弹出IM信息窗口,就无法触发通知~~ 
   # ~~ 对讲模式的时候，如果聊天记录为空，无法退出对讲模式~~ 
   # ~~ 腾讯微博的来源地址的解释需要重新弄一下，都变成链接了~~ 
   # ~~ IM的改名功能~~ 
   # ~~ 对方改名了，自己无法从服务器实时的看到~~ 
   # ~~ 加入通过邮件分享微博的功能 ~~
   # ~~ 私信发送变成发表新的微博 ~~
   # ~~ 完全禁用邮件功能，防止和BIS、BES冲突 ~~
   # ~~ 集成管理器（frame）加入了IM的配置选项 ~~
   # ~~ 解决Weibo模块当在主页显示整条Weibo的时候会崩溃的问题 ~~


== 1.8.1301更新内容==
   # ~~ 把语音定时器换成线程定时的（解决录音时间瞬间变没的问题） ~~
   # ~~ 酷黑版UI+气泡模式的显示问题~~ 
   # ~~ 删除最近联系人的功能~~ 
   # ~~ 保存语音、图像功能的配置加上~~ 
   # ~~ 重启丢失保存数据问题（其实就是在系统启动的时候试图访问SDCard的问题）~~ 
   # ~~ 短信界面的发微博的界面问题~~ 
   # ~~ 在聊天界面打开新的聊天界面的时候，输入框不会自动获得焦点~~ 


== 1.7.1289更新内容==
   # ~~ IM发送图片、文字、附件~~ 
   # ~~ 添加、删除、屏蔽好友~~ 
   # 群聊（添加丰富的自定义宏的功能）
   # ~~ 增加好友的搜索功能~~ 
   # ~~ 增加好友信息的查看~~ 
   # 状态信息栏需要扩充一下显示，不然120字看不完。
   # ~~ 主页微博加载更新的快捷键~~ 
   # 向下查看整个微博的快捷键sym，看看好不好实现。
   # ~~ Image按钮实现回车触发事件~~ 
   # ~~ 打开微博模块或者IM模块，进模块捣鼓一番，然后进设置关闭模块，保存返回，再进入语盒又会跳出那两个模块，必需要保存退出之后再重启语盒才行~~ 
   # ~~ 搞一个聊天输入框放在上面的选项，BB触摸屏的杯具。~~ 
   # 保存聊天记录到本地
   # ~~ 优化聊天的内存使用情况，研究 ForcedStackTraceException 问题(invokeLater 过多的问题)~~ 
   # ~~ 弹出IM窗口的两个键（回复/稍等）做快捷键~~ 
   # ~~ 貌似切换输入法可能会使用到Shift+Space，弄个其他额快捷键。~~ 
   # ~~ 向左指向的气泡的箭头有个小坑~~ 
   # ~~ 查看他人微博资料的时候客户端崩溃~~ 
   # ~~ 在签名处添加一个关键字，替换内容，实现固定格式的邮件。~~ 
   # ~~ 查看微博人资料的时候，右边的滚动条出现混乱没有刷新 ~~
   # ~~ 加入『对讲模式』~~ 
   # ~~ 选择表情的时候，右边加上标签，这是什么表情~~ 
   # ~~ 删除历史聊天记录的功能~~ 
   # ~~ 手机设置签名出了问题~~ 
   # ~~ BIS 和语盒EMail模块冲突了。SendMessage返回了false ~~
   # T 微博的直接RT功能
   # ~~ 检查一下6.0的语音，图片的发送情况 ~~
   # ~~ 检查一下从QQMail发HTML邮件，6.0系统显示出现问题的情况。 ~~
   # ~~ IM模块能发送，但是收不到的问题 ~~

== 1.6.1201更新内容 ==
   # ~~ 6.0 Weibo列表的焦点在刷新的Weibo的时候不会总是在屏幕里面，看看是否能改改进一下 ~~
   # ~~ 邮件和微博的是否发布地理位置信息的配置要分开 ~~
   # 需要查看一条微博的评论
   # ~~ 5.0的查看人物信息有UI上的BUG ~~
   # ~~ 上传文件，表情添加。 ~~
   # ~~ 回复自己发出的邮件，会发送不了。 ~~
   # ~~ 把那个时间字符串的高度写成16，不能减 ~~
   # 6.0打开地址的后面多一个 ％0 查查
   # 加一个时间同步功能
   # ~~ 气泡模式的私信显示很糟糕 ~~
   # ~~ 显示软件版本 ~~
   # ~~ 加上一个定时发送微博刷新消息的功能和配置。 ~~
   # ~~ 加上多邮箱的选择新建发送 ~~
   # ~~ 第一次安装后无法设置情景模式的问题 ~~
   # ~~ 多个账户中一个reset失败，所有账户都会卡死的bug ~~
   # ~~ 下载邮件附加的时候加入“取消下载”的选项 ~~
   # ~~ “快照”在9000设备下会失败 ~~
   # YuchIM
     # ~~ 弹窗提示 ~~
     # ~~ 加一个聊天记录上限的配置 ~~
     # ~~ 新聊天来了，往下滚没有滚到最下面 ~~
     # ~~ 测试断开的情况下发送消息，为何不是等待 ~~
     # ~~ 自动刷新好友列表仅仅只是一次 ~~
     # ~~ 新消息到的时候，如果是在聊天，就不要有新 ~~
     # ~~ 语盒用户需要标示出来。 ~~

== 1.5.1071更新内容 ==
   # ~~ 在列表里面查看本Weibo所有内容 ~~ 
   # ~~ 尽力做到解析内容里面的 http 连接 ~~
   # ~~ 在观看模式中 按T 也需要回到顶上 ~~
   # ~~ 6.0 的弹窗发Weibo，无法显示的问题 ~~
   # 试试Weibo私信放到消息文件夹里面看看。
   # ~~ 流量统计下面加一个复制到剪贴板的按钮和功能。 ~~
   # 修改一下Weibo推送顺序混乱的bug。
   # ~~ 转发、@ 的时候最大字数加大，不消除尾部，发送时自动截取，最好能显示字数。 ~~
   # ~~ 把QQ微博的URL提取一下，太坑爹了 ~~
   # ~~ 微博的来自那里，如果是空的，就不显示了，不过得看看为啥会为空 ~~
   # ~~ 评论的微博，如果是自己的，就别添加关注按钮了 ~~
   # ~~ QQ Weibo 无法看图，查查那个URL是否有效 ~~
   # ~~ 看看能否显示有图有真相的小图 ~~
   # ~~ Sina回复评论有些问题，貌似题不对文 ~~ 
   # ~~ 把断开提醒改成“若是链接不上，尝试5次后提醒” ~~ 
   # ~~ 保存数据的时候加一个提醒框，说正在“保存数据，请稍等……” ~~ 
   # ~~ 自己发出的RT，@ 不用提醒 ~~ 
   # 加一个选项，不保存历史Weibo到内存里面
   # 一键转发qq，t微博
   # ~~ 邮件附件列表需要排序一下，最近的文件放在前头 ~~ 
   # ~~ 保存打开图像时候的配置 ~~ 
   # ~~ YuchSign需要加一个重新发送激活邮件的按钮 ~~ 
   # ~~ 如果APN有空格，回车的时候要提醒，或者干脆禁用这两个字符 ~~
   # ~~ 列表模式的时候按E和F要起作用 ~~
   # ~~ 6.0 转发收不到附件的问题，查查 ~~
   # ~~ 主页上的微博也要发声提醒 ~~
   # ~~ 最终解决了回复、转发无原文的问题，同时前缀无限增加的问题也解决了 ~~
   # 增加一个查看后台正在下载的附件列表及进度
   # ~~ 收藏成功后需要提醒一下 ~~
   # ~~ 微博时间改成倒计时的时间 ~~
   # ~~ 0 空格 的快捷键做一个配置吧 ~~
   # ~~ 集成管理器需要加一个添加加密密码字段的功能啊 ~~
   # ~~ config 加一个选项，不输出log日志的配置 ~~
   # ~~ 集成管理器加入可以方便添加微博授权的功能 ~~

== 1.3.926更新内容==
   # ~~新发送推可以加入快捷弹出新窗口，这个任何时候发推都不会影响查看倒推地标记 ~~
   # 已读与未读标记
   # ~~在消息模块中增加菜单，可以发Weibo ~~
   # ~~ 发完Weibo之后，成功需要提示一下。 ~~
   # 看看能否显示有图有真相的小图
   # ~~ 浏览查看Weibo列表时，需要去掉上面的标题栏 ~~
   # ~~5.0版本的头像需要显示50像素大小的 ~~
   # ~~ 收到回复、评论时图标要变 ~~
   # ~~ 看看能不能使用代理模式授权访问 ~~
   # ~~ 更改加密密码的界面为命令行界面 ~~
   # ~~ 当有Item正在编辑时不增加，不删除 ~~
   # ~~ qq的认证不是V，改一下 ~~
   # ~~ 解决一下如何手动刷新配置的问题 ~~
   # ~~ 删除自己的评论可能会出问题 ~~
   # ~~ 发完weibo，判断，如果是手动刷新的，就要刷新一次 ~~
   # ~~ 发送邮件的抄送、迷送方收不到邮件 ~~
   # ~~ 增加YB断开提醒的配置和功能。 ~~

== 1.2.875更新内容 == 
   # 增加 config.xml里面的 yuchberry标签，具体干什么，你懂得。
   # 更新提示机制有问题啊，如果之前旧的服务器获得了更新标志，新的客户端链接上来，就会报错，所以，必须在每次链接上来的时候，服务器去取更新标志。
   # ~~ 使用“答复时丢弃原文”的时候，不替换“答复：”等前缀了。 ~~
   # ~~ 解决自驾服务器时config.xml明文密码的问题！~~
   # 试图解决：Server chose unsupported or disabled protocol: SSLv3 （zhxu@neusoft.com）其实就是增加STL的支持。区分一下smtp，smtps(sl),smtpt(ls)
   # POP3 不支持复制已发送邮件到文件夹（javax.mail.MethodNotSupportedException: Append not supported）
   # 英文版的 YuchSign 验证码。
   # ~~ 同一个邮件中有相同名字的附件，会出现问题。 ~~
   # 看一下能不能加入在程序里面设置工作目录的选项，方便linux用户。
   # ~~ 中文名+邮件地址 发送邮件时，会出现乱码的问题~~
   # ~~ qq信箱的IMAP无法连接的问题 (可以使用imap 的143端口连接了)~~
   # ~~ 6.0 系统转发邮件没有原文 ~~
   # ~~ 客户端主机地址中的空格需要去掉，或者提示 ~~
   # ~~ 发送带有附件的邮件的时候，会在手机本地将内容重复一遍。~~
   # 解决一下[http://code.google.com/p/yuchberry/issues/detail?id=158 这个Issue]
   # yuchweibo
    # ~~ Space下翻到最后条时页面变灰无法选择条目~~
    # ~~ 头像只能展开才能看见可以提到合并时就可以看见~~
    # ~~ 一旦有推送新条目就白屏~~
    # ~~ Timeline与@me只能看6条 （9700 OS6）~~
    # ~~ debug info 里面使用帮助菜单弹出浏览器会出错。高级设置、用户状态也有可能。~~
    # ~~ 翻到对上面的时候不应该出现发weibo的窗口（Home）~~
    # ~~ 使用一次性计算空格的长度，保证“关注（L）”“--来自于”不重叠~~
    # ~~ 出一个配置，可以显示、隐藏“--来自于”和“转发”“@Weibo”“收藏”几个按钮。~~
    # ~~做一个是否需要下载头像的配置，以适应小内存的机器 ~~
    # ~~加一个滚动条 ~~
== 1.1.715 更新内容 == 

   # ~~ 修改流量统计时候的递归调用，重复增加TCP连接时候的流量消耗 ~~
   # ~~ 增加“发送邮件”、“接收邮件”的统计功能。~~
   # ~~ 增加“诊断信息”可以复制到剪切板的功能。~~
   # ~~ 将每个邮件推送账户都有发件名称的功能，而不是赤裸裸的邮件地址。~~
   # ~~ 增加移动网络自动切换到wifi的功能。~~ (需要找人测试一下)
   # ~~ HTML 转化去掉多余的换行，去掉注释。 ~~
   # ~~ 当“答复：Re”混合时，就会出现问题，回复邮件的时候，系统“答复”前缀就不会自动添加了。~~
   # ~~ 增加一个Email字段，可以填写发件人的名字默认是空 ~~
   # ~~ 增加一个字段标记是否在服务器启动的时候查收之前的未读邮件。~~
   # ~~ 长按挂机键关机之后\GPRS服务不可用（也没有wifi），事实上YB还在运行，但是没有信号了，需要探测是否有信号，才能开启连接 ~~
   # 有些机器型号在接受数据的时候，没有办法发送数据，需要避免一下，虽然全双工的TCP不是这样的。
   # ~~ 加上“答复”是否带原文的选项。因为这些都是要流量的。~~
   # ~~ 加上一个选项，删除邮件的时候，把邮箱里面的邮件也删除了 ~~
   # ~~ 发送一封邮件，就可以更改服务器的签名 ~~
   # ~~ 增加“集成配置工具”可以手动更改到期时间 ~~
   # ~~ 增加 最多接受多少字节的邮件的功能。 ~~
   # ~~ 修改一下这个[http://code.google.com/p/yuchberry/issues/detail?id=124&colspec=ID%20Type%20Status%20Pri